package org.example.Models;

import java.util.ArrayList;

public class Cat extends Pets {

    public Cat(String birthday, ArrayList<String> commands, String name) {
        super(birthday, commands, name);
    }
}
